<!DOCTYPE html>
<html>
  <head>
    <title>Little Esty Shop - Final</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="top-container">
      <h1>Little Esty Shop - Final</h1>
      <nav>
        <ul>
          <li><%= link_to 'Admin Dashboard', admin_dashboard_index_path %></li>
          <li><%= link_to 'Merchants Index', merchants_path %></li>
        </ul>
      </nav>
      <div class="container">
        <% flash.each do |type, notice| %>
          <h5 class="text-info"><b><%= notice %></b></h5>
        <% end %>
      </div>
      <div class="container">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
